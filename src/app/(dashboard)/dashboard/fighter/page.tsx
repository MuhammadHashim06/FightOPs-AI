import { RoleSectionPlaceholder } from "@/features/dashboard/components/role-section-placeholder";

export default function FighterDashboardPage() {
  return (
    <RoleSectionPlaceholder
      eyebrow="Fighter"
      title="Fighter dashboard"
      description="This space can be used for bout status, required documents, schedules, readiness tasks, and communication updates for each fighter."
    />
  );
}
